public class Cluster {
	
	int minimum;
	int clusterstart;
	int clusterend;

	public Cluster(int minimum, int clusterstart, int clusterend) {
	
		this.minimum = minimum;
		this.clusterstart = clusterstart;
		this.clusterend = clusterend;	
	}
}